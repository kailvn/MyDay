# 个人日程管理应用｜最终需求文档 v3.0

版本：v3.0  
日期：2026-09-15  
定位：个人使用、离线优先、Ubuntu GNOME 优先的轻量日程 / 待办 / 记录工具。  
技术栈：**Tauri + Rust**。  
核心目标：**快速录入，少编辑，支持截图，可靠提醒，能跟踪记录，避免忘事；提供完整 CLI，可被其他程序或 agent 调用。**

---

## 0. 本次修订要点

1. **新增完整 CLI**：不仅是 `quick-add`，而是可被 agent 调用的结构化命令行接口。
2. **技术栈确定为 Tauri + Rust**：Rust 核心库 + Tauri GUI + CLI 二进制。
3. **CLI 与 GUI 共享核心逻辑**：统一数据模型、统一存储层、统一提醒逻辑。
4. **CLI 支持 JSON 输入输出**：方便 agent 解析和调用。
5. **GUI 与 CLI 通过本地 IPC 协同**：CLI 可通知运行中的 GUI 刷新或弹出窗口；GUI 未运行时 CLI 可直接写库。

---

## 一、使用场景分析

### 场景 A：早晨规划今天
用户打开主窗口，看到今天日程、待办、记录时间线。  
可以快速调整时间、标记待办完成、补一条记录。  
关键需求：今天视图、待办清单、日历视图、快速修改。

### 场景 B：临时会议，截图后立刻记录
用户截图，按全局快捷键，弹窗出现。  
输入标题，时间默认“下一个整点”，按 `Ctrl+V` 粘贴截图，回车保存。  
关键需求：全局快捷键、快速弹窗、UI 选时间、剪贴板图片、少点击。

### 场景 C：待办提醒，避免忘事
用户想到一件事，按快捷键新建待办。  
选择截止日期，设置提醒，到点 GNOME 通知。  
点击通知可打开详情，或直接“完成”“稍后提醒”。  
关键需求：待办、截止时间、系统通知、通知操作、去重。

### 场景 D：记录跟踪事件
用户想跟踪体重、吃药、头痛、咖啡、阅读、会议纪要。  
按快捷键新建记录，选模板，填数值或文本，保存。  
之后在时间线回看，在统计中看趋势。  
关键需求：记录模板、时间线、标签、数值、图片、统计。

### 场景 E：Agent 调用
用户让 agent 帮忙安排会议、添加待办、查询今天日程。  
Agent 调用 CLI，传入 JSON，获取结构化结果。  
关键需求：CLI 命令、JSON 输出、退出码、幂等操作、本地 IPC。

### 场景 F：晚间回顾
用户查看今天所有日程、已完成待办、记录。  
补写总结，安排明天待办。  
关键需求：日视图、时间线、搜索、快速追加。

### 场景 G：搜索与回顾
用户想找“上次客户沟通的截图”。  
通过搜索标题、备注、标签、日期范围找到日程，查看附件。  
关键需求：全文搜索、标签筛选、附件预览。

### 场景 H：备份与迁移
用户复制数据目录到新机器，恢复数据库和图片附件。  
关键需求：SQLite + 相对路径附件目录，一键备份。

### 从场景推导出的设计原则
1. **快**：全局快捷键、弹窗秒开、回车保存。
2. **少编辑**：时间智能默认，UI 点选，不强制填写。
3. **不依赖解析**：时间不用键盘输入，避免解析失败。
4. **统一收件箱**：日程、待办、记录都能快速添加。
5. **提醒可靠**：后台常驻，系统通知，点击可操作。
6. **可跟踪**：记录模板 + 时间线 + 统计。
7. **可编程**：完整 CLI + JSON，方便 agent 调用。
8. **离线可控**：本地数据库，图片文件目录，不依赖云。

---

## 二、产品定位与用户

### 产品定位
单人离线日程管理工具，兼顾日程、待办、记录。  
优先适配 Ubuntu GNOME，Wayland 下可用。  
基础功能完全离线，不依赖外部账号。  
提供 GUI 与 CLI 双入口，共享同一套本地数据。

### 用户角色
- Ubuntu GNOME 桌面个人用户。
- 习惯截图后立刻新建条目。
- 希望减少鼠标点击和文字编辑。
- 需要提醒避免忘事，也需要记录跟踪事件。
- 可能使用 agent 或脚本自动化操作日程。

---

## 三、核心概念：三类对象

| 类型 | 含义 | 时间字段 | 提醒 | 主要视图 |
|---|---|---|---|---|
| 日程 Event | 未来要发生的事 | 开始、结束、全天 | 有 | 日历月/周/日 |
| 待办 Task | 需要完成的事 | 截止日期/时间 | 有 | 今天、即将到期、全部、已完成 |
| 记录 Log | 已发生的事件，用于跟踪 | 发生时间，可选时长 | 通常无，可习惯提醒 | 时间线、模板、统计 |

统一数据模型，用 `type` 区分。  
日程完成后可生成记录；待办完成后自动写完成记录；记录可关联日程或待办。

---

## 四、P0 必须实现

### 1. 全局快捷键与快速添加

#### 1.1 唤起方式
- 应用提供命令：`myapp quick-add`。
- 用户在 GNOME 设置 → 键盘 → 自定义快捷键中绑定。
- 建议三个快捷键：
  - `Super+Alt+N`：新建日程
  - `Super+Alt+T`：新建待办
  - `Super+Alt+L`：新建记录
- 也可以同一个快捷键唤起，弹窗内用 `Ctrl+1/2/3` 切换类型。
- 应用单实例，通过本地 IPC 接收命令并弹出快速窗口。
- 后台常驻用 systemd user service 或 autostart。

#### 1.2 快速弹窗布局
弹窗极简，一屏内完成：

```
[ 日程 | 待办 | 记录 ]        Ctrl+1/2/3

标题： [____________________]

时间： [今天 15:00 ▾]   时长： [1小时 ▾]
提醒： [提前10分钟 ▾]
标签： [#工作] [#会议]
图片： [缩略图]  Ctrl+V 粘贴

[保存]  Enter
```

不同模式自动变化：
- 日程：开始、结束、提醒、全天。
- 待办：截止日期、优先级、完成状态。
- 记录：发生时间、模板、数值字段、标签。

#### 1.3 保存行为
- 标题是唯一必须键盘输入的内容。
- 时间、提醒、标签、类型尽量用 UI 选择。
- `Enter` 保存并关闭。
- `Esc` 取消。
- 主流程不依赖文本解析时间。

---

### 2. CLI 接口（新增，P0）

#### 2.1 目标
提供完整命令行工具，可被其他程序、脚本、agent 调用。  
与 GUI 共享核心库，保证数据一致。  
支持结构化输入输出，方便自动化。

#### 2.2 命令设计
建议命令：

```bash
myapp event add --title "项目评审" --start "2026-09-16T14:30" --end "2026-09-16T15:30"
myapp event list --date 2026-09-16 --json
myapp event get <id> --json
myapp event update <id> --title "新标题"
myapp event delete <id>

myapp task add --title "买牛奶" --due "2026-09-16" --remind "2026-09-16T09:00"
myapp task list --status todo --json
myapp task complete <id>
myapp task snooze <id> --until "2026-09-16T10:00"

myapp log add --title "体重" --value 70.5 --unit kg --tag 健康
myapp log list --from 2026-09-01 --to 2026-09-15 --json

myapp quick-add --type event --title "临时会议"

myapp search "客户沟通" --json
myapp export ics --output ~/calendar.ics
```

#### 2.3 输入输出规范
- 默认人类可读输出。
- `--json` 输出 JSON，字段稳定，方便 agent 解析。
- JSON 结构示例：
  ```json
  {
    "ok": true,
    "data": {
      "id": "evt_123",
      "type": "event",
      "title": "项目评审",
      "start": "2026-09-16T14:30:00+08:00",
      "end": "2026-09-16T15:30:00+08:00",
      "tags": ["工作"],
      "attachments": []
    }
  }
  ```
- 错误输出：
  ```json
  {
    "ok": false,
    "error": {
      "code": "NOT_FOUND",
      "message": "Event not found"
    }
  }
  ```
- 退出码：0 成功，1 一般错误，2 参数错误，3 未找到，4 冲突。

#### 2.4 与 GUI 的通信
- CLI 优先通过本地 IPC（Unix domain socket 或 DBus）通知运行中的 GUI。
- 若 GUI 未运行，CLI 直接操作 SQLite，GUI 下次启动时加载。
- GUI 运行中时，CLI 创建/修改后，GUI 收到通知并刷新视图。
- 提醒服务由 GUI 后台常驻或独立守护进程负责；CLI 只负责数据操作。

#### 2.5 Agent 调用建议
- Agent 使用 `--json` 获取结构化数据。
- 支持 `--dry-run` 预览。
- 支持 `--idempotency-key` 避免重复创建。
- 支持从 stdin 读取 JSON：
  ```bash
  echo '{"title":"买牛奶","due":"2026-09-16"}' | myapp task add --stdin --json
  ```

---

### 3. 时间选择器设计（重点）

目标：**不用键盘输入时间，避免解析失败，同时不能比打字慢。**

#### 3.1 默认智能
快速弹窗打开时，时间已有默认值：
- 日程：下一个整点或下一个半小时。
- 待办：今天，可选具体时间。
- 记录：现在。
- 用户可直接输入标题回车，完全不用碰时间。

#### 3.2 面板布局
点击时间按钮后展开单层面板，不打开新窗口：

- 第一行：日期快捷  
  `今天 | 明天 | 后天 | 下周一 | 选择日期…`
- 第二行：迷你日历  
  点击日期即选，当前日期高亮。
- 第三行：时间快捷  
  `现在 | +30分钟 | +1小时 | 09:00 | 12:00 | 14:00 | 18:00 | 20:00 | 最近使用`
- 第四行：时间网格  
  小时 0–23，分钟 `00 / 15 / 30 / 45`。  
  点小时再点分钟，自动应用并关闭。
- 第五行：时长快捷  
  `30分钟 | 1小时 | 2小时 | 到下一个整点 | 自定义结束`
- 全天开关。

#### 3.3 交互规则
- 点快捷时间直接应用并关闭，不再点“确定”。
- 点日期只改日期，时间保持默认。
- 点时间只改时间，日期保持默认。
- 不用滚轮选小时分钟。
- 不用长下拉列表。
- 不强制选分钟，默认 00 或 30。
- 记忆上次使用时间，优先显示。
- 根据当前时间和历史习惯推荐时间。
- 点击外部或 `Esc` 取消，不保存误触。
- `Enter` 确认，鼠标点击也能完成。

#### 3.4 三类对象差异
- 日程：开始 + 结束，默认 +1 小时。
- 待办：截止日期，快捷 `今天 | 明天 | 本周 | 无截止`。
- 记录：默认现在，快捷 `现在 | 5分钟前 | 30分钟前 | 自定义`。

#### 3.5 键盘辅助
- `Tab` 在标题、时间、提醒、标签、附件间跳。
- 方向键在日期和时间网格中移动。
- `Enter` 确认，`Esc` 取消。
- `Ctrl+V` 粘贴图片。
- `Ctrl+1/2/3` 切换类型。
- 键盘是加速键，不是必填路径。

---

### 4. 截图 / 剪贴板图片

- 快速弹窗和详情页都支持 `Ctrl+V` 粘贴图片。
- 自动保存到本地附件目录：
  `~/.local/share/myapp/attachments/<item_id>/<uuid>.png`
- 数据库只存相对路径：`attachments/<item_id>/<uuid>.png`。
- 支持多张图片、缩略图预览、删除。
- 删除条目时同步清理附件文件。
- 图片不存入数据库，不嵌入 ICS。

---

### 5. 日程功能

- 字段：标题、开始时间、结束时间、全天、备注、提醒时间、标签、附件。
- 支持日历月视图、周视图、日视图。
- 点击日期查看当日日程。
- 点击日程打开详情，查看文字和图片。
- 日程完成后可一键生成记录。
- 冲突检测：时间重叠时提示，但不阻止保存。

---

### 6. 待办功能

- 字段：标题、截止日期/时间、优先级、状态、备注、提醒、标签、附件。
- 视图：
  - 今天
  - 即将到期
  - 全部
  - 已完成
- 操作：
  - 完成 / 取消完成
  - 稍后提醒
  - 修改截止时间
  - 转为日程
- 完成时自动写一条完成记录。
- 通知支持“完成”“稍后提醒”。

---

### 7. 记录功能

#### 7.1 记录类型
- 文本记录：`头痛`、`会议纪要`
- 数值记录：`体重 70.5`、`血压 120/80`、`跑步 5km`
- 布尔记录：`吃药 ✅`、`喝咖啡 ✅`
- 时长记录：`阅读 30 分钟`、`冥想 10 分钟`
- 图片记录：截图、照片
- 组合记录：数值 + 标签 + 图片

#### 7.2 记录模板
- 模板示例：
  - 咖啡：标签“饮食”，计数 +1
  - 服药：标签“健康”，布尔
  - 体重：标签“健康”，数值 kg
  - 跑步：标签“运动”，距离、时长
  - 头痛：标签“健康”，强度 1–5，备注
- 快速记录弹窗显示模板按钮。
- 点模板，填数值或直接保存。
- 默认时间为现在。
- 回车保存到时间线。

#### 7.3 时间线视图
- 按天分组，倒序显示。
- 每条显示：时间、标题、标签、数值、图片缩略图。
- 支持快速追加。
- 支持筛选：标签、模板、日期范围、数值范围。

#### 7.4 统计与回顾
- 数值趋势图：体重、血压、跑步距离。
- 频率统计：咖啡、头痛、服药次数。
- 连续天数：习惯打卡。
- 热力图：一年记录密度。
- 每日/每周回顾自动汇总。

#### 7.5 记录提醒
- 记录本身通常不提醒。
- 可设置“记录习惯提醒”，例如每天 22:00 提醒写今日记录。
- 每周日提醒回顾。

---

### 8. 主窗口与视图

侧边栏：
- 日历：月/周/日
- 待办：今天、即将到期、全部、已完成
- 记录：时间线、模板、统计
- 搜索
- 设置

顶部全局搜索或命令面板：
- `Ctrl+K` 打开。
- 可搜日程、待办、记录、标签。
- 也可输入命令，如 `新建待办 买牛奶`。

---

### 9. 通知与提醒

- 后台常驻，每 30 秒扫描即将到期条目。
- 使用 DBus `org.freedesktop.Notifications` 发送 GNOME 系统通知。
- 日程提醒：提前 10 分钟、30 分钟、1 小时、自定义。
- 待办提醒：截止时间提醒，可稍后提醒。
- 记录习惯提醒：每天固定时间。
- 通知操作：
  - 日程：`打开`、`稍后提醒`
  - 待办：`完成`、`稍后提醒`
  - 记录：`追加记录`
- 点击通知打开应用并定位到对应条目。
- 若 GNOME 通知 action 受限，降级为打开应用并定位。
- 使用 `reminder_log` 去重，避免重复提醒。

---

### 10. 本地存储

- SQLite 存元数据。
- 图片存文件目录。
- 完全离线，不依赖云端账号。
- 支持新增、修改、删除。
- 使用事务和 WAL。
- 数据目录整体可复制备份和迁移。

建议表：
- `items`：所有条目，`type = event / task / log`
- `tags`、`item_tags`
- `attachments`
- `reminders`
- `templates`
- `settings`
- `reminder_log`

---

### 11. 搜索

- 按标题、备注、标签、时间范围检索。
- SQLite LIKE 或 FTS。
- 搜索结果可跳转到详情。
- 支持筛选类型：日程 / 待办 / 记录。

---

### 12. 设置

- 默认提醒时间。
- 工作时间、周起始日。
- 快速快捷键提示。
- 主题：跟随 GNOME 深浅色。
- 数据目录位置。
- 备份设置。
- CLI 是否启用、IPC 路径。

---

## 五、P1 建议实现

1. 重复日程：每日、每周，首版支持“修改全部”。
2. 数据备份：一键打包数据库和附件目录为 zip，保留最近 7 份。
3. ICS 导出：导出 `.ics`，备注中放 `myapp://item/<id>` 跳回本应用。
4. 统计图表：记录数值趋势、频率、热力图。
5. 模板管理：新增、编辑、排序记录模板。
6. 导入导出：记录导出 CSV / JSON / Markdown，待办导出 Markdown。
7. 收件箱：快速添加但未分类的条目，稍后整理。
8. 撤销：删除后 5 秒内可撤销。
9. CLI 扩展：批量操作、事务、更多过滤条件。

---

## 六、P2 后置

- 本地 ICS 订阅。
- 跨平台支持。
- 云端同步。
- 数据库或附件加密。
- 复杂重复规则、单次例外修改。
- 子任务。
- 多人协作。
- 富文本编辑器。
- 邮件、联系人等办公功能。

---

## 七、非功能需求

1. 运行环境：Ubuntu GNOME 优先，Wayland 需适配。
2. 性能：快捷键到弹窗 < 300ms，弹窗秒开，CLI 响应 < 100ms。
3. 存储：图片相对路径管理，数据库轻量。
4. 约束：基础功能完全离线。
5. 可靠性：删除清理附件，提醒去重，事务写入。
6. 单实例：重复快捷键不打开多个窗口。
7. 可访问性：键盘导航、屏幕阅读器。
8. 隐私：默认不联网，健康记录本地保存。
9. CLI 稳定性：命令参数和 JSON 字段保持向后兼容。

---

## 八、明确不做

1. 不做多人协作、云端同步。
2. 不把图片嵌入 ICS。
3. 不做复杂富文本编辑器。
4. 不需要邮件、联系人。
5. 首版不做复杂重复规则和单次例外修改。
6. 首版不依赖自然语言解析时间。
7. 不做远程 API 服务，CLI 和本地 IPC 足够。

---

## 九、技术栈与架构

### 9.1 技术栈
- **语言**：Rust
- **GUI**：Tauri 2.x + Web 前端（建议 Svelte 或 React，轻量优先）
- **核心库**：Rust crate，包含数据模型、存储、提醒、业务逻辑
- **CLI**：Rust 二进制，使用 clap 解析参数
- **数据库**：SQLite，通过 rusqlite 或 sqlx
- **通知**：notify-rust 或直接 zbus 调用 DBus
- **IPC**：Unix domain socket 或 DBus（zbus）
- **后台常驻**：Tauri 系统托盘 + 隐藏主窗口，或独立守护进程
- **全局快捷键**：Tauri global shortcut 插件 + GNOME 自定义快捷键 fallback
- **图片处理**：Rust image crate 或直接存储原始文件

### 9.2 架构分层
```
┌─────────────────────────────────────────┐
│  GUI (Tauri + Web 前端)                  │
├─────────────────────────────────────────┤
│  CLI (Rust binary)                       │
├─────────────────────────────────────────┤
│  核心库 (Rust crate)                     │
│  - 数据模型                              │
│  - 存储层 (SQLite)                       │
│  - 提醒调度                              │
│  - 附件管理                              │
│  - 搜索                                  │
│  - IPC 服务                              │
├─────────────────────────────────────────┤
│  SQLite + 附件目录                       │
└─────────────────────────────────────────┘
```

### 9.3 CLI 与 GUI 协同
- 核心库编译为 rlib，GUI 和 CLI 都依赖它。
- CLI 可直接操作数据库。
- GUI 运行时启动本地 IPC 服务（Unix socket 或 DBus）。
- CLI 优先通过 IPC 发送命令，由 GUI 执行并刷新界面。
- GUI 未运行时，CLI 直接写库；GUI 启动时加载。
- 提醒服务由 GUI 或独立守护进程负责，确保到点通知。

### 9.4 全局快捷键方案
- Tauri global shortcut 在 X11 可用，Wayland 可能受限。
- 推荐方案：GNOME 自定义快捷键调用 `myapp quick-add`。
- `myapp quick-add` 通过 IPC 通知 GUI 弹出快速窗口。
- 若 GUI 未运行，可先启动 GUI 再弹出。

---

## 十、验收标准

1. 快捷键唤起快速弹窗 < 300ms。
2. 快速弹窗中输入标题，回车即可保存，默认时间正确。
3. 时间选择器常用时间 3 次点击内完成。
4. 快速弹窗内 `Ctrl+V` 图片能保存，重启后可预览。
5. 月视图/周视图/日视图正常显示。
6. 待办今天、即将到期、全部、已完成视图正常。
7. 记录模板可保存，时间线可查看。
8. 提醒准时触发，GNOME 右上角弹出通知。
9. 通知可点击打开对应条目；待办通知可完成或稍后提醒。
10. CLI 支持 `--json`，能被 agent 调用并解析。
11. CLI 创建条目后，GUI 运行中能实时刷新。
12. GUI 未运行时，CLI 可直接写库，GUI 启动后可见。
13. 断网可用。
14. 数据目录整体备份后可恢复。
15. 删除条目后附件文件被清理。
16. 应用保持单实例。

---

## 十一、MVP 范围

首版只做 P0：
- 快速弹窗录入
- 时间选择器
- 日程 / 待办 / 记录三类对象
- 弹窗内粘贴图片
- 日历视图 + 待办清单 + 记录时间线
- GNOME 系统提醒
- SQLite + 本地附件目录
- 单实例 + GNOME 自定义快捷键
- 完整 CLI + JSON 输出
- 本地 IPC（CLI 与 GUI 协同）
- 基础搜索

P1 再做：
- 重复日程
- 备份
- ICS 导出
- 统计图表
- 模板管理
- 导入导出
- CLI 批量操作

P2 再评估：
- 本地 ICS 订阅
- 跨平台
- 云同步
- 加密
- 复杂重复规则

---

以上为最终版需求文档 v3.0。核心决策：**时间全部 UI 选择，主流程不依赖文本解析；日程、待办、记录统一模型；提醒围绕“避免忘事”设计；数据本地离线，附件相对路径；技术栈 Tauri + Rust；提供完整 CLI，可被 agent 调用。**
